buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // ... otras dependencias ...
        classpath 'com.google.gms:google-services:4.3.14' // Asegúrate de tener esta línea
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
